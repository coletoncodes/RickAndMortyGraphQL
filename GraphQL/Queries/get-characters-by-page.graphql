query Characters($page: Int) {
  characters(page: $page) {
    info {
      count
      next
      pages
      prev
    }
    results {
      gender
      id
      image
      status
      type
      species
      name
      origin {
        name
        dimension
        type
      }
    }
  }
}
