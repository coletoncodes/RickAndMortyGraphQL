query Episodes($page: Int) {
  episodes(page: $page) {
    info {
      count
      next
      pages
      prev
    }
    results {
      air_date
      created
      episode
      name
      id
    }
  }
}
